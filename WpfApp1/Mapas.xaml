<Window x:Class="UI.Mapas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Mapas" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock FontWeight="Bold" FontSize="16" Text="Estatísticas de Tickets" Margin="0,0,0,10" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10" >
                <TextBlock Text="Total Tickets: " FontWeight="Bold" Width="150"/>
                <TextBlock Text="{Binding TotalTickets}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Percentual Respondidos: " FontWeight="Bold" Width="150"/>
                <TextBlock Text="{Binding PercentualRespondidos}" />
            </StackPanel>

            <TextBlock FontWeight="Bold" Text="Média de Tempo por Tipo:" Margin="0,20,0,5"/>

            <DataGrid ItemsSource="{Binding MediaTempoAtendimentoPorTipoLista}" AutoGenerateColumns="False" Height="200" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="*"/>
                    <DataGridTextColumn Header="Média Tempo" Binding="{Binding MediaTempoFormatada}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>

</Window>
